#!/usr/bin/env bash

if [ ! -z "${DEV_BASE// }" ]
then
  cd $DEV_BASE/java/xlatticeUtil_java
  
  blListGen \
    -d ../xlatticeUtil_java \
    -1L  \
    -u $DVCZ_UDIR \
    $@
  
else
  echo "DEV_BASE is not defined"
fi
